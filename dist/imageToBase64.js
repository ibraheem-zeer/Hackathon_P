// const fs = require('fs');
// const path = require('path');
export {};
// // Function to read an image file and convert it to Base64
// function imageToBase64(filePath) {
//     try {
//         // Read the image file
//         const imageBuffer = fs.readFileSync(filePath);
//         // Convert the image buffer to Base64
//         const base64Data = imageBuffer.toString('base64');
//         return base64Data;
//     } catch (error) {
//         console.error('Error reading or converting the image:', error);
//         return null;
//     }
// }
// // Provide the path to the image file you want to convert
// const imagePath = path.join(__dirname, 'path_to_your_image.jpg'); // Replace with the actual path
// // Call the function to convert the image
// const base64Image = imageToBase64(imagePath);
// // Print the Base64 representation of the image
// if (base64Image) {
//     console.log('Base64 Image:', base64Image);
// }
